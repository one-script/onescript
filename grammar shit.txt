method = func id([id:] id, ...) [id] body

body = -> expr | block
block = statement | { statement... }
statement = |[

let[:id] id [= expr]
symbol(expr, ...)
symbol = expr
symbol[++ | -- ...unary]
symbol[+= | -= ...binary][expr]
return [expr]
while (expr) block
if (expr) block [else block]
for (statement; expr; statement) block
/* TODO: switch statement */

]|

/* method examples */
func a(b: c) -> c / 2
func d(e: f) {
	return f / 300
}